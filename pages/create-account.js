function createAccountPage() {
    return (
      <div>
        <h1>Create Account Page</h1>
        <h2>Create an account on Partage to access the exclusive features on our marketplace, like the user dashboard</h2>
      </div>
    );
  }
  
  export default createAccountPage;